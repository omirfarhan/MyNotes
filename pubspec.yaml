name: miniappflutter
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  #firebase_core: ^2.25.0   # Firebase initialize এর জন্য
  #firebase_auth: ^4.10.0   # FirebaseAuth ব্যবহার করার জন্য

  fluttertoast: ^8.2.5 # Use the latest version
  cloud_firestore: ^6.0.3
  firebase_analytics: ^12.0.3
  firebase_auth: ^6.1.1
  firebase_core: ^4.2.0
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  path: ^1.9.1




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

